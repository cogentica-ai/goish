// go: file crypto/internal/fips140/edwards25519/field/fe_amd64_noasm.go decls: feMul, feSquare
//
// The purego side of Go's build-tag pair — fe_amd64.go declares the same
// two symbols as assembly generated by avo. goish has no assembly, so
// this is the only side it can implement; see nistec and bigmod for the
// same decision.

#![allow(non_snake_case)]

use super::fe::Element;
use super::fe_generic::{feMulGeneric, feSquareGeneric};

// go: sdk 1.25.5 crypto/internal/fips140/edwards25519/field/fe_amd64_noasm.go:9-9 feMul
pub(super) fn feMul(v: &mut Element, x: &Element, y: &Element) {
    // Go: func feMul(v, x, y *Element) { feMulGeneric(v, x, y) }
    feMulGeneric(v, x, y);
}

// go: sdk 1.25.5 crypto/internal/fips140/edwards25519/field/fe_amd64_noasm.go:11-11 feSquare
pub(super) fn feSquare(v: &mut Element, x: &Element) {
    // Go: func feSquare(v, x *Element) { feSquareGeneric(v, x) }
    feSquareGeneric(v, x);
}
