// go: file crypto/internal/fips140/nistec/fiat/p521_invert.go decls: P521Element.Invert
//
// Code generated by addchain. DO NOT EDIT.
//
// Translated from Go by scripts/fiat_invert_to_rust.py. A transposed
// operand or an off-by-one loop bound here produces a wrong inverse
// silently, so the translation is mechanical and its parser raises on
// anything it does not recognise. Regenerate with scripts/fiat_gen.py.

#![allow(non_snake_case)]

use super::p521::P521Element;
use crate::types::int;

impl P521Element {
    // go: sdk 1.25.5 crypto/internal/fips140/nistec/fiat/p521_invert.go:12-89 P521Element.Invert
    /// Set e = 1/x, and return e.
    ///
    /// If x == 0, Invert returns e = 0.
    pub fn Invert(&mut self, x: &P521Element) -> &mut Self {
        // Inversion is implemented as exponentiation with exponent p − 2.
        // The sequence of 13 multiplications and 520 squarings is derived from the
        // following addition chain generated with github.com/mmcloughlin/addchain v0.4.0.
        //
        //	_10       = 2*1
        //	_11       = 1 + _10
        //	_1100     = _11 << 2
        //	_1111     = _11 + _1100
        //	_11110000 = _1111 << 4
        //	_11111111 = _1111 + _11110000
        //	x16       = _11111111 << 8 + _11111111
        //	x32       = x16 << 16 + x16
        //	x64       = x32 << 32 + x32
        //	x65       = 2*x64 + 1
        //	x129      = x65 << 64 + x64
        //	x130      = 2*x129 + 1
        //	x259      = x130 << 129 + x129
        //	x260      = 2*x259 + 1
        //	x519      = x260 << 259 + x259
        //	return      x519 << 2 + 1
        //
        let mut z = P521Element::New();
        z.Set(*self);
        let mut t0 = P521Element::New();
        z.Square(*x);
        z.Mul(*x, z);
        t0.Square(z);
        // Go: for s := 1; s < 2; s++
        let mut s: int = 1;
        while s < 2 {
            t0.Square(t0);
            s += 1;
        }
        z.Mul(z, t0);
        t0.Square(z);
        // Go: for s := 1; s < 4; s++
        let mut s: int = 1;
        while s < 4 {
            t0.Square(t0);
            s += 1;
        }
        z.Mul(z, t0);
        t0.Square(z);
        // Go: for s := 1; s < 8; s++
        let mut s: int = 1;
        while s < 8 {
            t0.Square(t0);
            s += 1;
        }
        z.Mul(z, t0);
        t0.Square(z);
        // Go: for s := 1; s < 16; s++
        let mut s: int = 1;
        while s < 16 {
            t0.Square(t0);
            s += 1;
        }
        z.Mul(z, t0);
        t0.Square(z);
        // Go: for s := 1; s < 32; s++
        let mut s: int = 1;
        while s < 32 {
            t0.Square(t0);
            s += 1;
        }
        z.Mul(z, t0);
        t0.Square(z);
        t0.Mul(*x, t0);
        // Go: for s := 0; s < 64; s++
        let mut s: int = 0;
        while s < 64 {
            t0.Square(t0);
            s += 1;
        }
        z.Mul(z, t0);
        t0.Square(z);
        t0.Mul(*x, t0);
        // Go: for s := 0; s < 129; s++
        let mut s: int = 0;
        while s < 129 {
            t0.Square(t0);
            s += 1;
        }
        z.Mul(z, t0);
        t0.Square(z);
        t0.Mul(*x, t0);
        // Go: for s := 0; s < 259; s++
        let mut s: int = 0;
        while s < 259 {
            t0.Square(t0);
            s += 1;
        }
        z.Mul(z, t0);
        // Go: for s := 0; s < 2; s++
        let mut s: int = 0;
        while s < 2 {
            z.Square(z);
            s += 1;
        }
        z.Mul(*x, z);
        return self.Set(z);
    }
}
