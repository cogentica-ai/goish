// math — Go's `math` parent package.
//
// v1 currently exposes the math/bits subpackage. The full math
// package (Pi/E constants, Sin/Cos/Sqrt, etc.) lands later.

#![allow(non_snake_case)]

pub mod bits;
